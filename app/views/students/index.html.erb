<h1> Student List View </h1>
<%= form_tag students_path, method: :get do %>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag 'Search' %>

<% end %>

<%= @students.each do |stud_obj| %>
    <%= render 'student', {student: stud_obj} %>
<% end %>   